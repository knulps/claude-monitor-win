[claude]
# F12 → Network → click /usage request → Request Headers → copy the full cookie value
cookies =

# F12 → Network → the /organizations/{UUID}/ segment in the URL
org_id  =

# API refresh interval (seconds)
poll_interval = 60

[ui]
# UI language: en | ko
language = en
